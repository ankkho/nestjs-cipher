import {describe, expect, it} from 'vitest';
import {CipherHealthIndicator} from './cipher.health';

describe('CipherHealthIndicator', () => {
  it('should be defined', () => {
    expect(CipherHealthIndicator).toBeDefined();
  });
});

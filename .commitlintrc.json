{
  "extends": [
    "@commitlint/config-conventional"
  ],
  "rules": {
    "scope-enum": [
      2,
      "always",
      [
        "kms",
        "health",
        "module",
        "types",
        "deps",
        "docs"
      ]
    ]
  }
}